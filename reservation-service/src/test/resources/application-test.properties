# Logging
logging.level.org.mongodb=INFO
feign.client.config.default.loggerLevel=basic
feign.client.config.establishmentarea-service.loggerLevel=basic
feign.client.config.customer-service.loggerLevel=basic

# MongoDB
spring.data.mongodb.host=localhost
spring.data.mongodb.port=37017
spring.data.mongodb.database=reservation

# Servlet
server.port=17001

# Swagger
springdoc.swagger-ui.enabled=false

# Loadbalancer - This is permanent
spring.cloud.loadbalancer.ribbon.enabled=false

# Eureka
spring.cloud.service-registry.auto-registration.enabled=false
eureka.client.enabled=false
eureka.client.serviceUrl.registerWithEureka=false

# Discovery
spring.cloud.discovery.enabled=false

# Zipkin
spring.zipkin.enabled=false

# Sleuth
spring.sleuth.sampler.percentage=1.0
spring.sleuth.web.skipPattern=(^cleanup.*|.+favicon.*)

# Feign
feign.circuitbreaker.enabled=true
feign.client.config.establishmentarea-service.connectTimeout=250
feign.client.config.establishmentarea-service.readTimeout=500
feign.client.config.customer-service.connectTimeout=250
feign.client.config.customer-service.readTimeout=500

# Integration
reservation.integration.port=80
reservation.establishmentarea.service.url=localhost:${reservation.integration.port}
reservation.customer.service.url=localhost:${reservation.integration.port}

# Business
res.reservation.category.fields-to-escape=
res.reservation.association.fields-to-escape=
res.reservation.engagement.fields-to-escape=
res.reservation.engagement.date.format=dd-MM-yyyy
res.reservation.engagement.time.format=HH:mm:ss
res.reservation.engagement.timestamp.format=dd-MM-yyyy~HH:mm:ss